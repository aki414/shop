import { ShoesColorPipe } from './shoes-color.pipe';

describe('ShoesColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ShoesColorPipe();
    expect(pipe).toBeTruthy();
  });
});
